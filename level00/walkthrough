1. We firstly saw that the executable waiting for a userinput and compare the result with 149c
2. The userinput is taken from a scanf that wait for an int.
3. If the userinput is equal to 149c, it spawns a shell ! So we just had to enter 5276 to have a shell as level1 
